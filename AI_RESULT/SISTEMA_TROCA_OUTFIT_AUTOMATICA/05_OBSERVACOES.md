# 05_OBSERVACOES

- A lista `config.outfitLookTypes` pode ser expandida com outros lookTypes válidos do servidor.
- Caso queira apenas variar cores, basta remover alteração de `lookType` em `generateRandomOutfit`.
- O sistema foi mantido sem mudança de protocolo/client para reduzir risco e manter compatibilidade.
